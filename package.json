{
  "name": "chorus",
  "version": "0.0.0",
  "private": true,
  "description": "A TypeScript-first, LLM-agnostic multi-agent framework",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=20"
  }
}